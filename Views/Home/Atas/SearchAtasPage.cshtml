@{
    ViewData["Title"] = "Sistema FDE";
}

<!--ESTILO DOS FORMULÁRIOS-->
<style>
    body {
        background-color: skyblue;
        font-family: Gadugi;
    }

    .page_title {
        text-align: center;
    }

    #title_ata_search {
        margin: 150px 0 30px 0;
        color: whitesmoke;
        font-family: 'Courier New';
        font-weight: 800;
        text-align: center;
        margin: 30px 0px 30px 0;
        text-shadow: 2px 2px 4px #000000;
    }

    #form_atas {
        border: solid 10px;
        border-color: #0360AB;
        font-weight: 800;
        margin: 0 auto;
        width: 800px;
        height: 600px;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        background-color: #59B0F9;
        border-radius: 10px;
        padding: 20px;
    }

    #form_pedidos {
        border: solid 10px;
        border-color: #0360AB;
        font-weight: 800;
        margin: 0 auto;
        margin-bottom: 400px;
        width: 800px;
        height: 800px;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        background-color: #59B0F9;
        border-radius: 10px;
        padding: 20px;
    }

    input {
        margin: 20px 0px 30px 0px;
        width: 800px;
        height: 40px;
    }

    .form-group {
        margin-bottom: 10px;
    }

    .form-control {
        border-radius: 5px;
        border-color: #B3DFFD;
    }

    .btn-primary {
        margin: 0 auto;
        text-align: center;
        margin-top: 10px;
        font-weight: 800;
        width: 400px;
        height: 80px;
        border-radius: 5px;
        background-color: #AB221D;
        border: solid 5px;
        border-color: #89CFF0;
    }
</style>



<!--FORMULÁRIO DE PESQUISA DE ATAS-->
<h1 class="page_title" id="title_ata_search">Pesquisa de Ata</h1>

<form id="form_atas" method="post" action="/Home/ProcessarSearchAtas">
    <div class="form-group">
        <label for="numeroAta">Número da Ata:</label>
        <input type="text" class="form-control" id="numeroAta" name="numeroAta" placeholder="xx/xxxxx/xx/xx-xxx" maxlength="18" />
    </div>
    <div class="form-group">
        <label for="numeroAno">Número do Ano:</label>
        <input type="text" class="form-control" id="numeroAno" name="numeroAno" placeholder="2023" maxlength="4" />
    </div>
    <button type="submit" class="btn btn-primary">Pesquisar</button>
</form>


<!--SCRIPTS PARA MANIPULAR OS FORMULÁRIOS E SEUS CAMPOS-->
<script>

    const input = document.querySelector('#numeroAta');

    input.addEventListener('keypress', () => {

        let inputlength = input.value.length

        if (inputlength === 2 || inputlength === 8 || inputlength === 11) {
            input.value += '/'
        } else if (inputlength === 14) {
            input.value += '-'
        }
    })

</script>
